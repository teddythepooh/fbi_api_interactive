name: Keep Alive

on:
  schedule:
    - cron: '0 */11 * * *'
  workflow_dispatch:


jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -s https://teddythepooh.streamlit.app/ > /dev/null
